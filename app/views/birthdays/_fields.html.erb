<% if @birthday.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@birthday.errors.count, "error") %> prohibited this birthday from being saved:</h2>

    <ul>
    <% @birthday.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= f.label 'data' %>
<%= f.date_select :date, start_year: Date.today.year-110, end_year: Date.today.year %>

<%= f.label 'imię' %> 
<%= f.text_field :name %>

<%= f.label 'nazwisko' %>
<%= f.text_field :surname %>

<%= f.label 'pseudonim' %>
<%= f.text_field :nick %>

