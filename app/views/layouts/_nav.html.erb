<nav>
	<ul>
		<% if user_signed_in? %>
			<li><%= link_to current_user.email, user_path(current_user) %></li>
			<li><%= link_to 'wyjdź', destroy_user_session_path, method: :delete %></li>
			<li><%= link_to 'dodaj', new_user_birthday_path(current_user) %></li>
		<% end %>
	</ul>
</nav>

